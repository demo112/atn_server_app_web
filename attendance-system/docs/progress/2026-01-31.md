# 2026-01-31 进展日志

## 完成事项

### 1. 排班管理 (SW65) 功能开发
- [x] **Server**: 完成排班核心服务 `ScheduleService`，支持冲突检测与强制覆盖（Force Mode）。
- [x] **Server**: 完成批量排班接口 `BatchCreateSchedule`，支持按部门批量创建。
- [x] **Server**: 完成单元测试与集成测试，覆盖率满足要求。
- [x] **Web**: 完成排班日历视图 `ScheduleCalendar`，解决时区显示问题。
- [x] **Web**: 完成单人排班弹窗 `ScheduleDialog`，支持冲突提示与强制覆盖。
- [x] **Web**: 完成批量排班弹窗 `BatchScheduleDialog`。

### 2. 关键问题修复
- **时区问题**: 前端日历比较日期时改用 `YYYY-MM-DD` 字符串比较，修复了时区导致的数据显示偏差。
- **跨天冲突策略**: 确认并实现了“新排班优先融合”策略，允许物理时间跨天重叠，仅检测管理日期冲突。

## 下一步计划
- [ ] 推进 SW66 补签处理功能。
- [ ] 准备集成测试环境 (E2E)。
