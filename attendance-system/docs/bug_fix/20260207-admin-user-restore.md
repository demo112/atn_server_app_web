# Admin 用户恢复记录

## 问题描述
- **现象**：系统中缺少默认的 `admin` 管理员账号。
- **复现步骤**：查询数据库用户表，未发现 `username='admin'` 的记录。
- **影响范围**：无法使用默认管理员账号登录系统。

## 设计锚定
- **所属规格**：系统初始化 / 用户管理
- **原设计意图**：系统初始化时应包含一个默认的管理员账号（admin/123456）以便初始配置。
- **当前偏离**：该账号缺失。

## 根因分析
- **直接原因**：数据库中不存在 `admin` 用户记录。
- **根本原因**：可能是之前的测试数据清理操作误删了该账号，或者初始化脚本未完全执行。
- **相关代码**：`packages/server/seed_admin.ts`

## 修复方案
- **修复思路**：执行现有的 `seed_admin.ts` 脚本，重新创建或更新 `admin` 账号。
- **改动文件**：无代码修改，仅执行数据修复脚本。

## 关联组件
| 组件 | 文件路径 | 是否同步修复 |
|------|----------|--------------|
| N/A | N/A | N/A |

## 验证结果
- [x] 原问题已解决：`list-users.ts` 显示 `admin` 用户已存在。
- [x] 回归测试通过：不涉及代码变更，风险极低。
- [x] 设计一致性确认：符合系统设计。

## 文档同步
- [ ] design.md：无需更新
- [ ] api-contract.md：无需更新

## 防回退标记
**关键词**：admin账号, seed数据
**设计决策**：始终保留 admin 账号作为系统兜底管理员。

## 提交信息
chore(server): restore admin user data
