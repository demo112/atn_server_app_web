# 文档一致性修复计划

根据您的指示，我将执行以下文档修复操作：

## 1. 请假流程对齐 (SW67)
**目标**: 移除所有“审批流”相关描述，统一为“管理员录入生效”模式。
- **操作**:
    - 解锁 `docs/requirement-analysis.md` 和 `docs/requirements.md`。
    - 修改 `docs/requirement-analysis.md`: 删除 "4.4 请假审批流程" 图表，更新 "4.3 请假状态" (移除待审批/已拒绝)。
    - 修改 `docs/requirements.md`: 更新 SW67 描述，更新决策记录 (审批流程 -> ❌ 否)。

## 2. 补齐 Web 班次管理 (SW64)
**目标**: 补充 Web 端班次管理的详细需求。
- **操作**:
    - 在 `docs/features/SW64/` 下创建新文档 `requirements_web.md`。
    - 定义 Web 端班次的增删改查 (CRUD) 需求、时间段配置交互及校验规则。

## 3. 报表需求对齐 (SW72)
**目标**: 扩充特性文档，覆盖核心文档定义的所有报表。
- **操作**:
    - 修改 `docs/features/SW72/requirements.md`。
    - 补充 User Stories: 日报表、年报表、考勤异常报表。
    - 确保与 `requirements.md` 中定义的 SW72 范围完全一致。

## 4. 重新锁定
- **操作**: 修复完成后，重新锁定核心需求文档，防止意外修改。

请确认执行此计划。
