---
inclusion: always
---

# 文档管理规范

## 目录结构

```
attendance-system/docs/
├── api-contract.md          # API 契约（全局）
├── database-design.md       # 数据库设计（全局）
├── requirements.md          # 需求规格（全局）
├── requirement-analysis.md  # 需求分析（全局）
├── project-roadmap.md       # 项目路线图
├── task-backlog.md          # 任务清单
├── changelog.md             # 变更日志
├── deployment.md            # 部署文档
├── progress/                # 进展日志（按日期）
│   └── YYYY-MM-DD.md
└── features/                # 功能文档（按规格编号）
    └── {SPEC_ID}/
        ├── requirements.md  # 需求文档
        ├── design.md        # 设计文档
        └── tasks.md         # 任务拆分
```

## 命名规范

### 功能目录命名

使用规格编号作为目录名，全大写：

| 正确 ✅ | 错误 ❌ |
|---------|---------|
| `SW62/` | `sw62/` |
| `SW63/` | `SW63_考勤配置/` |
| `UA1/` | `UA1_UserManagement/` |

### 文档文件命名

统一使用英文 kebab-case：

| 正确 ✅ | 错误 ❌ |
|---------|---------|
| `requirements.md` | `需求文档.md` |
| `design.md` | `设计文档.md` |
| `tasks.md` | `任务拆分.md` |
| `alignment.md` | `对齐文档.md` |

### 进展日志命名

使用日期格式 `YYYY-MM-DD.md`：

| 正确 ✅ | 错误 ❌ |
|---------|---------|
| `2026-01-31.md` | `进展-01-31.md` |

## 文档模板

### 功能需求文档 (requirements.md)

```markdown
# {SPEC_ID} - {功能名称}

## 概述

{一句话描述功能目标}

## 用户故事

- 作为{角色}，我希望{功能}，以便{价值}

## 功能需求

### FR-1: {需求名称}

{需求描述}

## 非功能需求

{性能、安全等要求}

## 验收标准

- [ ] {验收条件1}
- [ ] {验收条件2}
```

### 设计文档 (design.md)

```markdown
# {SPEC_ID} - 设计文档

## 技术方案

{方案描述}

## 数据模型

{表结构或类型定义}

## API 设计

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/v1/xxx | xxx |

## 流程图

{关键流程说明}
```

### 任务拆分 (tasks.md)

```markdown
# {SPEC_ID} - 任务拆分

## 任务列表

| ID | 任务 | 负责人 | 状态 |
|----|------|--------|------|
| T1 | xxx | sasuke | ⬜ |
| T2 | xxx | naruto | ⬜ |

状态：⬜ 待开始 | 🔄 进行中 | ✅ 已完成
```

## 创建新功能文档

当开始新功能开发时：

1. 在 `attendance-system/docs/features/` 下创建规格目录
2. 创建三个标准文档：`requirements.md`、`design.md`、`tasks.md`
3. 按模板填写内容

```bash
# 示例：创建 SW65 功能文档
attendance-system/docs/features/SW65/
├── requirements.md
├── design.md
└── tasks.md
```

## 禁止事项

❌ 不要在功能目录名中加中文描述
❌ 不要使用中文文件名
❌ 不要在 docs 根目录创建功能相关文档
❌ 不要创建嵌套的重复目录结构

## 迁移指南

现有文档需要迁移到新结构：

| 旧路径 | 新路径 |
|--------|--------|
| `docs/SW62/` | `docs/features/SW62/` |
| `docs/SW63/` | `docs/features/SW63/` |
| `docs/SW64/` | `docs/features/SW64/` |
| `docs/UA1_UserManagement/` | `docs/features/UA1/` |
| `docs/database-sync-comments/` | `docs/features/database-sync-comments/` |

中文文件名需要重命名：

| 旧文件名 | 新文件名 |
|----------|----------|
| `对齐文档.md` | `alignment.md` |
| `共识文档.md` | `consensus.md` |
| `任务拆分.md` | `tasks.md` |
| `设计文档.md` | `design.md` |
